[workspace]
members = ["orrery-sidecar"]
resolver = "2"

[profile.release]
# A sidecar ships inside the installer, so size matters more than the last
# few percent of speed.
opt-level = 3
lto = true
codegen-units = 1
strip = true
